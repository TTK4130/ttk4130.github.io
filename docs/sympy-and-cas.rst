==============
Sympy and CAS
==============

.. tabs::

   .. code-tab:: py

         def main():
             return
             
   .. code-tab:: matlab

      function [avg, med] = mystats(x)
      n = length(x);
      avg = mymean(x,n);
      med = mymedian(x,n);
      end


